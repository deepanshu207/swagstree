rules_version = '2';

// Swagstree storefront rules (swagstree-web) — merge snippet only.
// Your full production rules live in Firebase Console for swagstree-web.
//
// Shipping Optimizer extension data is on Firebase project extension-e6e32.
// Deploy firestore.extension.rules to that project (NOT swagstree-web).
//
// Legacy: if shipping_optimizer_* collections still exist on swagstree-web, you may
// remove them after migrating data to extension-e6e32.

service cloud.firestore {
  match /databases/{database}/documents {
    // ... your existing Swagstree rules (products, orders, settings, etc.) ...
  }
}
